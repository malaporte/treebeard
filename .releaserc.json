{
  "branches": ["main"],
  "tagFormat": "v${version}",
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    "@semantic-release/changelog",
    ["@semantic-release/exec", {
      "prepareCmd": "cd packages/treebeard && node -e \"const p=require('./package.json'); p.version='${nextRelease.version}'; require('fs').writeFileSync('package.json', JSON.stringify(p, null, 2) + '\\n')\""
    }],
    "@semantic-release/github",
    ["@semantic-release/git", {
      "assets": ["packages/treebeard/package.json", "CHANGELOG.md"],
      "message": "chore(release): ${nextRelease.version} [skip ci]"
    }]
  ]
}
